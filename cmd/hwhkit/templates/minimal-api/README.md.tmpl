# {{.ProjectName}}

Generated by `hwhkit init`. Built on [hwhkit-go](https://github.com/hwhkit/hwhkit-go).

## Quick start

```bash
go mod tidy
make run
curl http://localhost:8080/hello
```

## Endpoints (OOTB)

- `/health` — liveness
- `/health/ready` — readiness (probes all registered integrations)
- `/metrics` — Prometheus exposition
- `/version` — git SHA / build time / Go version
- `/info` — config sources + integration state
